server:
  port: 9999
  servlet:
    context-path: /tunamoi

spring:
  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/Tuna
    username: postgres
    password: Tuananhqp2005@
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher